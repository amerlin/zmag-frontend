import React from "react";
import { useSelector } from "react-redux";
import { AppState } from "../../Data/Store";

export const OrderGrid = ({ props: any }) => <div>OrderGrid</div>;
export default OrderGrid;
